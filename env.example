# Solana Development Workspace Configuration
# Copy this file to .env and customize for your environment.

# Docker volume name for persistent workspace storage (optional).
# Defaults to 'solana-workspace' if not specified.
SOLANA_VOLUME_NAME=solana-workspace

# Resource limits applied by docker compose (optional).
SOLANA_DEV_MEMORY=4g
SOLANA_DEV_CPUS=2.0

# Solana cluster (mainnet-beta, devnet, testnet, localhost)
SOLANA_CLUSTER=devnet

# Custom RPC endpoint (optional)
# SOLANA_RPC_URL=https://api.devnet.solana.com
